# Testing

Run `npm run typecheck`, `npm run lint`, `npm test`, and `npx expo-doctor`. Manual QA: onboarding, offline restart, project create, dark mode, TalkBack labels, project detail, malformed-import handling, database migration, notification permission rationale, export/import, and large project lists.
